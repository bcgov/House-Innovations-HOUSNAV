import "./{{ pascalCase name }}.css";

export default function {{ pascalCase name }}({ children }: { children: React.ReactNode }) {
  return (
    <div>
      <h1 className="ui-{{ pascalCase name }}">{{ pascalCase name }} Component</h1>
      {children}
    </div>
  );
};
